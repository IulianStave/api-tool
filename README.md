# api-tool
The default configuration JSON file is 'local.config.json' and it should follow the 'config.json' structure.
In case you have another configuration file it should be passed as command line parameter, for instance:

python3 api.py myconfig.json

 
